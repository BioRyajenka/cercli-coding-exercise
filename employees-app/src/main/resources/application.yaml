ktor:
  deployment:
    port: 8080
    watch:
      - classes
      - resources
  development: true
  application:
    modules:
      - com.example.ApplicationKt.main

jwt:
  secret: "secret"
  expected-issuer: "Auth Server"
